name: My first workflow
on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome message
              run: echo "my first github actions job"

            - name: List Files
              run: ls
            
            - name: Read the files
              run: cat file.txt
               

